version: '3.8'

services:
  ocr-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - TESSDATA_PREFIX=/usr/share/tesseract-ocr/4.00/tessdata
    volumes:
      - ./test_files:/app/test_files
